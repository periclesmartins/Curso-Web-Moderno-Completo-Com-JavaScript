// Define uma constante chamada 'raio' e atribui o valor 5.6 a ela
const raio = 5.6;

// Define uma constante chamada 'area' e atribui a ela o valor calculado a partir da fórmula A = π * r^2
const area = Math.PI * Math.pow(raio, 2);

// Imprime o valor da constante 'area' no console
console.log(area);

// Imprime o tipo de dado da propriedade 'Math' no console
console.log(typeof Math);
