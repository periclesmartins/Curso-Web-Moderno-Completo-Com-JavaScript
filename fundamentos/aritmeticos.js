const [a,b,c,d] = [3,5,1,15] //criação de um array com quatro variáveis e atribuição de valores a cada uma delas

const soma = a + b + c + d //soma dos valores de todas as variáveis
const subtracao = d - b //subtração dos valores de d e b
const multiplicacao = a * b //multiplicação dos valores de a e b
const divisao = d / a //divisão do valor de d pelo valor de a
const modulo = a % 2 //resto da divisão do valor de a por 2

console.log(soma,subtracao,multiplicacao,divisao,modulo) //imprime no console os resultados das operações realizadas acima
